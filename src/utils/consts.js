const tabContentIds = {
  logs: 'first-logs',
  feeds: 'second-feeds',
  flickr: 'third-flickr',
};

const classNames = {
  hide: 'hide',
  list: 'list',
  listItem: 'list__item',
  photosGallery: 'photos-container',
  photo: 'photos-container__photo',
};

const styles = {
  block: 'block',
};

const hash = {
  first: '#first',
  second: '#second',
  third: '#third',
};

const headers = {
  feeds: 'feeds',
  logs: {
    hostnames: 'top hostnames',
    files: 'top files',
  },
};

const consts = {
  classNames,
  tabContentIds,
  styles,
  hash,
  headers,
};

export {
  consts as default,
  classNames,
  tabContentIds,
  styles,
  hash,
  headers,
};
